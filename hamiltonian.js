


//this function takes in an array variable, containing atomic orbital basis
function fillMatrix(basisArray){
    //hamil is the matrix of hamiltonian integrals 
    var hamil = [][];
    //inverseOverlap is the inverse of the overlap matrix, used to satisfy the secular equation (Ĥ*S^-1)*Ψ = E* Ψ
    var inverseOverlap = [][];
    
}



